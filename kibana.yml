---
elasticsearch:
  password: VFBDiCcgjUtuOntIFriT
  ssl:
    certificateAuthorities: ${CERTS_DIR}/ca/ca.crt
  username: kibana_system
server:
  host: 0.0.0.0
  ssl:
    certificate: ${CERTS_DIR}/${NODE_NAME}/${NODE_NAME}.crt
    enabled: 'true'
    key: ${CERTS_DIR}/${NODE_NAME}/${NODE_NAME}.key